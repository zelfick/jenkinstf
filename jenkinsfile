pipeline{
    agent any
    environment{
        PATH= "${PATH}:${getTerraformPath()}"
    }
    stages{
        stage('terraform init'){
            steps{
                sh "terraform init"
            }

        }
    }
}

def getTerraformPath(){
    def tool name: '/home/jorgeherran/Documents/terraform/', type: 'org.jenkinsci.plugins.terraform.TerraformInstallation'
    return tfHome
}